import React from "react";

const statusButton = () => {
  //   const handleStatus = (status) => {
  //     setStatus(status);
  //     setstatusRow(row);
  //   };

  return (
    <>
      <div>
        {/* {status ? (
          <span class="badge bg-primary">Primary</span>
        ) : (
          <span class="badge bg-danger">Danger</span>
        )} */}
        <span class="badge bg-primary green">Primary</span>
      </div>
    </>
  );
};

export default statusButton;
